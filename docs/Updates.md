## Bug
When a user makes changed to a config, then saves, then switches
to another user (which should be fine since the user already saved),
the system still shows a warning. Then, if the user tries again, it allows them without the warning.
There seems to be no affect on the data files. 
